# TaskBridge Project Memory

- 2026-06-10: CLI output unification is tracked under `openspec/changes/taskbridge-cli-output-polish/`, not ad hoc docs. The change defines `internal/clioutput` as the ordinary-command projection/renderer contract for summary/json/agent/events/explain, with default human CLI chrome in English and machine fields stable English.
- 2026-06-10: `taskbridge analyze priority` is the first stats-panel acceptance example. Default human output renders a boxed stats panel with Urgent/High/Medium/Low/No priority rows and a `Total | Active | Completed` footer; legacy `--format json` remains parseable while global `--json` uses envelopes where routed through projections.
- 2026-06-10: `taskbridge agent *` remains separate from ordinary command `--agent`; it keeps `taskbridge.agent-result.v1` JSON. Ordinary command `--agent` is low-token key=value output via `internal/clioutput`.
- 2026-06-10: CLI output polish final contract: ordinary commands keep default human/table/summary output even when piped; explicit `--json` uses `internal/clioutput` envelopes, `--agent` uses key=value, unsupported `--events` returns an error, and `--explain` renders reviewable Conclusion/Evidence/Risks/Next sections. `list`, `lists`, `config show`, `config validate`, and `analyze` are routed through this global-mode dispatcher.
- 2026-06-10: User-facing CLI chrome should be English. Task tables use English headers/status/quadrant/priority labels; control-plane today/next/inbox/review titles and reasons are English; Feishu provider display name is `Feishu Tasks`; human status labels may include icons but machine output must not.

- 2026-06-15: `taskbridge demo today` implemented as the no-auth demo entry point, reusing `today --mock`'s mock service. `doctor` and `quickstart` now recommend `taskbridge demo today` instead of `today --mock` to users. `today --mock` remains as a test/compatibility path.
- 2026-06-15: `internal/actionaudit` service added. Every `review --apply-file` and `agent execute` attempt writes a receipt under `<storage.path>/audit/actions/<session_id>.json` with schema `taskbridge.action-audit.v1`. Fields: schema_version, session_id, command, dry_run, confirm, status, started_at, finished_at, duration_ms, stats, operations, errors, redaction. Read via `taskbridge audit show <session-id>` or `taskbridge audit list`.
- 2026-06-15: `agent execute` failure now returns non-zero exit code. stdout still has valid JSON envelope, stderr gets human diagnostic. This satisfies the agent contract: "stdout has valid JSON, failure returns non-zero exit code".
- 2026-06-15: Root command repositioned from "CLI workflow tool for AI and multi-Todo platforms" to "Local task execution control plane for humans and agents". Help text uses English-first Get Started section pointing to `doctor -> demo today -> today -> next -> review -> agent capabilities`. OmniFocus and Apple Reminders removed from help (not yet implemented).
- 2026-06-15: Taskfile descriptions translated to English. `test:integration` now covers DemoToday, Doctor, Quickstart, Contract, Audit, and AgentExecute test patterns.
- 2026-06-15: `taskbridge-control-plane-hardening` ALL tasks (0.1-7.4) completed and verified. OpenSpec validates strict. `go test ./...` (26 packages), `go test -race` (storage/sync/projectservice/actionaudit), `go vet`, `go build`, `gofmt -l`, `git diff --check`, `task test:integration` all pass.
- 2026-06-15: `agent capabilities` now declares audit support, output behavior, confirmation rules, and `not_implemented` list (mcp_adapter, remote_provider_write, bidirectional_sync_auto_resolve). Schema versions index includes all 8 schemas. `agent schemas` outputs validator-friendly `taskbridge.agent-schema-index.v1` with name/description/commands per schema.
- 2026-06-15: `docs/agent-contract.md` updated with exit codes table, audit receipt section, schema index section, and MCP adapter deferral boundary. Roadmap marked with 2026-06-15 hardening scope note. AGENTS.md includes `task test:integration` gate for CLI process e2e.
- 2026-06-15: Deferred items (require separate OpenSpec changes): MCP adapter (thin wrapper, post-contract-stable), remote Provider audit, bidirectional sync auto-resolve, project auto-adjust, Apple Reminders/OmniFocus providers.

- 2026-06-15: `taskbridge-code-quality-refactor` ALL 29 tasks completed. Key deliverables this batch: `internal/analyze` package created with CalculateQuadrant/Priority/Time/Trend/Report; `cmd/analyze.go` refactored to use type aliases and delegate computation; format contract tests for table/compact/markdown/tsv; store-init regression tests for list/lists/task add; shared output helper contract tests.
- 2026-06-15: `english-cli-output-contract` ALL 27 tasks completed. README.md, AGENTS.md, Taskfile.yml fully translated to English.
- 2026-06-15: Implemented `taskbridge-release-distribution`. Release config now uses versioned archives, `checksums.txt`, archive SPDX SBOMs, nFPM direct assets, Homebrew cask `yeisme/homebrew-tap/Casks/taskbridge.rb`, Scoop `yeisme/scoop-bucket/bucket/taskbridge.json`, and snapshot skip-upload. Release workflow separates `${{ secrets.GITHUB_TOKEN }}` from cross-repo `PUBLISHER_TOKEN`, prefers `actions/create-github-app-token@v3`, and adds snapshot/tag binary smoke. Post-release workflow validates archive checksums, `--version`, `--help`, `demo today`, Homebrew cask install, and gated Scoop smoke. Root `--version` flag is supported for package-manager smoke. Verification observed: red/green `go test ./cmd -run TestRootVersionFlag -count=1`, `task release:check`, `task release:local`, release artifact checksum/binary smoke, workflow YAML parse, `task test`, `task build`, `task test:integration` evidence `temp/integration-test-runs/20260615T120904Z-1788151/summary.json`, `task check`, and `openspec validate taskbridge-release-distribution --strict` all passed. OpenSpec task 4.3 records the external blocker for first real tag Homebrew/Scoop evidence: no semver tag has been published from this unmerged working tree.
